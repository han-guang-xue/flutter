#include <stdio.h>
#include <stdlib.h>

#include <windows.h>

#define maxsize 100
int main(int argc, char const *argv[])
{

    MessageBox(NULL, TEXT("这个是我的第一个程序"), TEXT("C 桌面程序"), MB_OK);

    // int map[maxsize][maxsize];
    // int i = 0;
    // for (i = 0; i <= maxsize + 1; i++)
    // {
    //     if (i == 0 || i == maxsize + 1)
    //     {
    //         printf("H");
    //     }
    //     else
    //     {
    //         printf("*");
    //     }
    // }
    // return 0;
}
